# docker
This is a simple package, intended to run in a Docker container. It listens on a UNIX socket for connections from NintendoChannelWeb, and runs the file generator with the requested arguments.